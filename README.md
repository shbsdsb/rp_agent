# rp-agent

AI 角色扮演 agent 平台(长期愿景:取代 SillyTavern 的本地独立工具)。
当前为第一阶段:CLI 骨架。

## 开发环境

- Python >= 3.14
- UV 包管理

## 快速开始

```bash
uv sync
uv run rp-agent --version
uv run rp-agent hello
uv run pytest
```
