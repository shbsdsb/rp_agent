# **调用头脑风暴skill，添加和修改命令**

# API 连接管理命令集重构设计（最终版）

> 本设计文档整合了原有命令集的优化建议，以 **安全性、可扩展性、易用性** 为核心，重新定义所有子命令及交互行为。实施时请严格遵循。

---

## 1. 全局设计原则

- **命名参数优先**：所有需要参数的子命令均支持 `--name`、`--url`、`--key`、`--model` 等长选项，位置参数作为简写形式保留但优先推荐命名参数。
- **职责单一**：每个子命令只做一件事，组合使用可实现复杂流程。
- **交互安全**：敏感信息（API Key）在输入时静默，存储时加密，显示时脱敏。
- **错误友好**：明确的错误信息，网络请求设超时，支持重试提示。

---

## 2. 命令总览

```bash
api <command> [options] [arguments]
```

| 命令     | 用途                             |
| -------- | -------------------------------- |
| `list`   | 列出所有连接（支持详细视图）     |
| `get`    | 查看单个连接详情（密钥脱敏）     |
| `add`    | 新建连接（不覆盖已有）           |
| `del`    | 删除连接                         |
| `test`   | 测试连接可用性                   |
| `pull`   | 拉取模型列表（测试或已存连接）   |
| `sync`   | 测试连接并拉取模型，可选设为默认 |
| `modify` | 交互式或非交互式修改连接         |

---

## 3. 子命令详细规范

### 3.1 `api list` —— 列出连接

**用法**  
```bash
api list [--verbose|-v] [--filter <key>=<value>]
```

- 默认只显示连接名称（简洁模式）。
- `--verbose` / `-v`：以表格形式显示所有连接的 `名称`、`Base URL`、`当前模型`、`最近测试时间`。
- `--filter`：按字段筛选，如 `--filter model=gpt-4`。

---

### 3.2 `api get <name>` —— 查看详情

**用法**  
```bash
api get <name>
```

- 显示该连接的所有字段（`base_url`、`model`、`api_key` 脱敏显示，只露前4后4字符）。
- 若连接不存在，报错。

---

### 3.3 `api add` —— 新建连接（不覆盖）

**用法**  
```bash
api add --name <name> --url <base_url> --key <api_key> [--model <model>]
```

- **位置参数简写**：`api add <name> <base_url> <api_key> [model]`（仍支持，但推荐使用命名参数）。
- 若 `<name>` 已存在，**报错并退出**，提示使用 `modify` 修改。
- 新建后自动保存（密钥加密存储）。
- 可选 `--model` 指定默认模型，若不指定则为空。

**扩展**：  
- 若同时指定 `--pull`（如 `api add --name ... --url ... --key ... --pull`），则在保存前先尝试连接并拉取模型列表，打印模型名，但不设置默认模型（需后续通过 `modify` 设置）。若拉取失败则中止保存。

---

### 3.4 `api del <name>` —— 删除连接

**用法**  
```bash
api del <name>
```

- 删除前二次确认（交互式输入 `y/N`）。
- 支持 `--force` 跳过确认。

---

### 3.5 `api test <name>` —— 测试连接

**用法**  
```bash
api test <name> [--timeout <seconds>]
```

- 使用当前配置发送一个轻量请求（如 `/models` 或自定义探测）验证连通性。
- 返回成功/失败信息，失败时显示错误原因。
- `--timeout` 自定义超时（默认 10s）。

---

### 3.6 `api pull` —— 拉取模型列表

**用法**  
```bash
# 对已保存的连接
api pull <name> [--set-default]   # 拉取后自动将第一个模型设为默认（可选）

# 临时测试（不保存）
api pull --url <base_url> --key <api_key> [--timeout <seconds>]
```

- 无论哪种用法，均打印返回的模型名列表（编号或名称）。
- 对于已存连接，拉取结果不影响原有配置，除非指定 `--set-default` 或后续手动修改。
- 失败时给出明确错误（网络、认证、格式等）。

---

### 3.7 `api sync` —— 测试并拉取模型（一体化）

**用法**  
```bash
api sync <name> [--set-default]
```

- 依次执行：测试连通性 → 拉取模型列表 → 打印模型列表。
- 若指定 `--set-default`，则**自动将拉取列表中的第一个模型设为该连接的默认模型**（需用户确认，或直接设定后提示）。
- 若测试失败，终止执行，不拉取。

---

### 3.8 `api modify` —— 修改连接（交互式或非交互式）

**用法**  
```bash
# 交互式编辑（推荐人工操作）
api modify <name> [--interactive|-i]

# 非交互式快速修改（适合脚本）
api modify <name> --set <field>=<value> [--set ...]
```

**交互式编辑（默认）**：
- 执行 `api modify <name>` 即进入交互模式（等同于 `api modify <name> -i`）。
- 提示符变为 `rp-agent-modify-<name>>`。
- 显示当前各字段（密钥脱敏）。
- 依次询问 `Base URL`、`API Key`（静默输入）、`Model`，直接回车保留原值。
- 支持快捷指令：
  - `save`：保存当前修改并退出。
  - `exit` 或 `..`：放弃所有修改并退出。
  - `reset`：在当前会话中重置所有字段为原始值。
  - `/field`：跳转到指定字段（如 `/model` 直接修改模型）。
- 保存时校验 URL 格式和非空，失败则重输。

**非交互式修改**：
- 使用 `--set field=value` 一次修改一个或多个字段，例如：
  ```bash
  api modify my-conn --set model=gpt-5 --set base_url=https://new.url
  ```
- 若字段不存在或值非法，报错并退出（不保存）。
- 密钥可通过 `--set key=newkey` 设置，输入时可在命令行明文（注意历史记录），也可通过环境变量 `API_KEY` 传入（推荐）。

> **注意**：原有的 `api <name> -m` 简写形式依然支持，内部映射至 `api modify <name>`。

---

## 4. 安全与存储

- API Key 存储使用系统密钥环（如 `keyring` 库）或加密配置文件（AES-256），禁止明文存储。
- 显示时脱敏：仅展示前4位和后4位，中间用 `****` 代替。
- 交互式输入密钥时，采用 `getpass` 不显示输入内容。

---

## 5. 扩展性与兼容性

- 所有命令支持 `--help` 显示详细帮助。
- 保留旧版位置参数顺序（`add <name> <base_url> <model> [api_key]`）作为过渡，但输出弃用警告，并建议改用命名参数。
- 未来增加新字段（如 `headers`、`timeout`）时，统一通过 `--set` 或交互式扩展，无需修改命令结构。

---

## 6. 错误处理与日志

- 网络请求设置默认超时（10s），可全局配置。
- 所有操作记录日志（`~/.rp-agent/logs/api.log`），方便排查。
- 交互式操作中意外中断（如 Ctrl+C）不保存修改，保持数据一致性。

---

## 7. 实施检查清单

- [ ] 实现命名参数解析（使用 argparse 或 click）。
- [ ] 实现密钥加密存储与读取。
- [ ] 实现交互式编辑界面（含快捷指令）。
- [ ] 实现 `pull` 临时测试功能。
- [ ] 实现 `sync` 组合功能。
- [ ] 实现 `list --verbose` 表格输出。
- [ ] 编写单元测试覆盖所有子命令及边界情况。
- [ ] 更新 `--help` 和用户文档。

---



> 如有任何疑问或需要调整，请及时沟通。