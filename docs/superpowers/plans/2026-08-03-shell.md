# 交互式 Shell 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 rp-agent 添加 `shell` 子命令:交互式 REPL(help/config/reload/storage/hello/history/exit),供用户测试命令。

**Architecture:** 新增 `src/rp_agent/shell.py`:纯函数 `parse_line` + 可注入 `_input` 的 `run_shell` 主循环 + 模块级命令表 `_COMMANDS`(handler 签名 `Callable[[list[str]], None]`);历史存模块级 `_history`,`run_shell` 开始时清空。`cli.py` 注册 `shell` 子命令。

**Tech Stack:** Python 3.14、标准库(logging/typing)、pytest。

## Global Constraints

- Python >= 3.14;测试一律 `uv run pytest`(Windows,不用 python3)
- 不新增依赖(标准库实现,禁止 prompt_toolkit/readline 依赖);`uv.lock` 不变
- 提示符 `rp-agent> `;`exit`/`quit` 退出;Ctrl+C / Ctrl+D 正常退出;空行忽略
- 命令 handler 签名统一 `(args: list[str]) -> None`;`parse_line` 用 `strip()` + `split()`(不支持引号分组)
- 现有 30 项测试保持通过
- 工作分支 `feat/shell`

---

### Task 1: `shell.py` — REPL 主循环与命令集

**Files:**
- Create: `src/rp_agent/shell.py`
- Create: `tests/test_shell.py`

**Interfaces:**
- Consumes:
  - `from rp_agent.config import get_config, reload_config`(现有)
  - `from rp_agent.storage import DATA_DIR, ensure_dirs`(现有)
- Produces:
  - `parse_line(line: str) -> tuple[str, list[str]]`
  - `run_shell(_input: Callable[[str], str] = input) -> None`
  - 命令集:`help`/`?`、`config`、`reload`、`storage`、`hello`、`history`;`exit`/`quit` 在循环内处理

- [ ] **Step 1: 写失败测试 `tests/test_shell.py`**

```python
from rp_agent.shell import parse_line, run_shell


def _feed(lines: list[str]):
    """返回注入式 _input:按序返回行,耗尽后抛 EOFError。"""
    it = iter(lines)

    def _inner(_prompt: str) -> str:
        try:
            return next(it)
        except StopIteration:
            raise EOFError from None

    return _inner


def test_parse_line():
    assert parse_line("  hello  world  ") == ("hello", ["world"])
    assert parse_line("") == ("", [])
    assert parse_line("   ") == ("", [])


def test_run_shell_sequence(capsys):
    run_shell(_feed(["hello", "config", "exit"]))
    out = capsys.readouterr().out
    assert "你好" in out
    assert "log_level" in out
    assert "退出" in out


def test_run_shell_unknown_command(capsys):
    run_shell(_feed(["foobar", "exit"]))
    out = capsys.readouterr().out
    assert "未知命令" in out


def test_run_shell_eof(capsys):
    run_shell(_feed([]))  # 立即 EOFError
    out = capsys.readouterr().out
    assert "退出" in out


def test_help_lists_commands(capsys):
    run_shell(_feed(["help", "exit"]))
    out = capsys.readouterr().out
    for name in ("help", "config", "reload", "storage", "hello", "history", "exit"):
        assert name in out
```

- [ ] **Step 2: 运行测试确认失败**

```bash
uv run pytest tests/test_shell.py -v
```
Expected: FAIL(`ModuleNotFoundError: No module named 'rp_agent.shell'`)

- [ ] **Step 3: 写 `src/rp_agent/shell.py`**

```python
"""交互式 shell:供测试命令的 REPL 输入口。零依赖。"""
from __future__ import annotations

import logging
from typing import Callable

from rp_agent.config import get_config, reload_config
from rp_agent.storage import DATA_DIR, ensure_dirs

logger = logging.getLogger("rp_agent")

PROMPT = "rp-agent> "
_BANNER = "rp-agent 交互式 shell —— 输入 help 查看可用命令,exit 退出"
_history: list[str] = []


def parse_line(line: str) -> tuple[str, list[str]]:
    """解析输入行 → (命令名, 参数列表);空行 → ("", [])。"""
    parts = line.strip().split()
    if not parts:
        return "", []
    return parts[0], parts[1:]


def _cmd_help(_args: list[str]) -> None:
    print("可用命令:")
    for name, (desc, _handler) in sorted(_COMMANDS.items()):
        print(f"  {name:<10} {desc}")
    print("  exit/quit  退出 shell")


def _cmd_config(_args: list[str]) -> None:
    cfg = get_config()
    print(f"log_level={cfg.log_level}")


def _cmd_reload(_args: list[str]) -> None:
    changed = reload_config()
    cfg = get_config()
    print(f"配置已重载,发生变化: {changed},log_level={cfg.log_level}")


def _cmd_storage(_args: list[str]) -> None:
    ensure_dirs()
    print(f"data 目录: {DATA_DIR}")
    for sub in sorted(p for p in DATA_DIR.iterdir() if p.is_dir()):
        items = sorted(p.name for p in sub.iterdir())
        print(f"  {sub.name}/: {items}")


def _cmd_hello(_args: list[str]) -> None:
    cfg = get_config()
    print(f"你好!rp-agent 骨架已就绪,当前日志级别: {cfg.log_level}")
    logger.info("shell 中执行 hello")


def _cmd_history(_args: list[str]) -> None:
    for i, h in enumerate(_history, start=1):
        print(f"  {i:>3}  {h}")


_COMMANDS: dict[str, tuple[str, Callable[[list[str]], None]]] = {
    "help": ("显示帮助", _cmd_help),
    "?": ("显示帮助", _cmd_help),
    "config": ("显示当前配置", _cmd_config),
    "reload": ("热重载配置", _cmd_reload),
    "storage": ("列出 data 目录内容", _cmd_storage),
    "hello": ("冒烟命令", _cmd_hello),
    "history": ("显示输入历史", _cmd_history),
}


def run_shell(_input: Callable[[str], str] = input) -> None:
    """交互式主循环。_input 可注入(测试用);Ctrl+C/Ctrl+D 正常退出。"""
    _history.clear()
    print(_BANNER)
    while True:
        try:
            line = _input(PROMPT)
        except (EOFError, KeyboardInterrupt):
            print("退出")
            return
        cmd, args = parse_line(line)
        if not cmd:
            continue
        if cmd in ("exit", "quit"):
            print("退出")
            return
        if line.strip() not in _history:
            _history.append(line.strip())
        entry = _COMMANDS.get(cmd)
        if entry is None:
            print(f"未知命令: {cmd}(输入 help 查看可用命令)")
            continue
        try:
            entry[1](args)
        except Exception:
            logger.exception("命令执行失败: %s", cmd)
            print(f"命令执行出错: {cmd}(详情见日志)")
```

- [ ] **Step 4: 运行测试确认通过**

```bash
uv run pytest tests/test_shell.py -v
```
Expected: 5 passed

- [ ] **Step 5: 提交**

```bash
git add src/rp_agent/shell.py tests/test_shell.py
git commit -m "feat: 交互式 shell REPL(shell.py:parse_line/run_shell/命令集)"
```

---

### Task 2: `cli.py` 注册 `shell` 子命令

**Files:**
- Modify: `src/rp_agent/cli.py`
- Modify: `tests/test_cli.py`

**Interfaces:**
- Consumes: `run_shell`(Task 1)
- Produces: `shell` Typer 子命令

- [ ] **Step 1: 追加失败测试 `tests/test_cli.py`**

```python
def test_shell_command_registered(monkeypatch):
    called: dict = {}
    monkeypatch.setattr("rp_agent.shell.run_shell", lambda: called.update(ok=True))
    result = runner.invoke(app, ["shell"])
    assert result.exit_code == 0
    assert called.get("ok") is True
```

- [ ] **Step 2: 运行测试确认失败**

```bash
uv run pytest tests/test_cli.py::test_shell_command_registered -v
```
Expected: FAIL(`No such command 'shell'`)

- [ ] **Step 3: 在 `src/rp_agent/cli.py` 的 `hello` 命令后追加**

```python
@app.command()
def shell() -> None:
    """进入交互式 shell(测试命令用)。"""
    from rp_agent.shell import run_shell

    run_shell()
```

- [ ] **Step 4: 运行测试确认通过**

```bash
uv run pytest tests/test_cli.py -v
```
Expected: 6 passed(原 5 + 新 1)

- [ ] **Step 5: 手动冒烟(输入 exit 立即退出)**

```bash
printf "help\nexit\n" | uv run rp-agent shell
```
Expected: banner、可用命令列表、退出。

- [ ] **Step 6: 提交**

```bash
git add src/rp_agent/cli.py tests/test_cli.py
git commit -m "feat: cli 注册 shell 子命令(交互式输入口)"
```

---

### Task 3: 全量验证与收尾

**Files:**
- Modify: `README.md`(补充 shell 用法)

**Interfaces:**
- Consumes: Task 1-2 产物
- Produces: 全绿测试套件 + 完整 git 历史

- [ ] **Step 1: 运行全量测试**

```bash
uv run pytest -v
```
Expected: 36 passed(30 原有 + 5 shell + 1 cli)

- [ ] **Step 2: 更新 README.md(在"数据存储"章节后追加)**

```markdown
## 交互式 Shell

```bash
uv run rp-agent shell
```

内置命令:`help`(帮助)、`config`(查看配置)、`reload`(热重载配置)、
`storage`(查看 data 目录)、`hello`(冒烟)、`history`(输入历史)、`exit`/`quit`(退出)。
```

- [ ] **Step 3: 确认工作树整洁并提交收尾**

```bash
git status --short
git add -A
git commit -m "chore: 交互式 shell 完成(README 更新)"
git log --oneline
```
Expected: `git log --oneline` 显示连续提交历史。

---

## 验收清单(对照 spec)

- [ ] `parse_line` 纯函数:空白剥离/参数切分/空行 → spec §3.2、§5
- [ ] `run_shell` 可注入 `_input`;Ctrl+C/Ctrl+D 正常退出 → spec §3.2、§4
- [ ] 命令集:help/config/reload/storage/hello/history;exit/quit;未知命令提示不退出 → spec §3.3
- [ ] `cli.py` 注册 `shell` 子命令 → spec §3.4
- [ ] 历史:会话内去重记录,`history` 命令可查 → spec §3.5
- [ ] 现有 30 项测试保持通过,总计 36 passed → spec §7
