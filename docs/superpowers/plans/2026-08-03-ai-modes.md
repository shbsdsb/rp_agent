# chat / rp / agent 三种 AI 工作模式(占位)实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 rp-agent 添加 chat / rp / agent 三个 CLI 子命令与对应 core 占位模块,并在交互 shell 中引入工作模式系统(home/chat/rp/agent),支持 `/` 转义调用正常命令、`/exit` 返回 home。

**Architecture:** shell.py 用单一 `mode` 状态驱动:前缀随模式变化,非 home 模式下非 `/` 输入视为对话内容(占位阶段打印灰色报错);`/` 开头的输入剥掉 `/` 后走既有 `_COMMANDS` 分派。core/chat.py、core/rp.py、core/agent.py 各导出 `run()`,调用 `run_shell(initial_mode=...)`;cli.py 注册三个子命令延迟导入 core 模块。

**Tech Stack:** Python ≥3.14、Typer、prompt_toolkit(已就绪,零新依赖)、pytest。

## Global Constraints

- Python >= 3.14,一律用 UV 包管理器:`uv run pytest -v` 跑测试,`uv run rp-agent` 运行
- 交互 REPL 输入必须用 prompt_toolkit(现有 `_read_line` 已实现,本计划不新增依赖)
- 日志只用标准库 logging(不新增 loguru 等)
- 占位报错文案必须用 `term.gray()` 包裹(非 tty / NO_COLOR 自动禁用,test 里 capsys 断言无 ANSI)
- 颜色:prompt_toolkit 无 `ansigray`,灰色用 `ansibrightblack`(现有 SHELL_STYLE 已用)
- 当前分支 `feat/ai-modes-placeholder`,小步提交,不直接提交 main
- 新增文件:`core/chat.py`、`core/rp.py`、`core/agent.py`、`tests/test_core.py`

---

### Task 1: shell 工作模式系统

**Files:**
- Modify: `src/rp_agent/shell.py`(PROMPT 常量、run_shell 主循环、_KNOWN_COMMANDS、_BANNER)
- Modify: `src/rp_agent/help_data.py`(HELP_ENTRIES 新增 chat/rp/agent、更新 exit 描述)
- Test: `tests/test_shell.py`、`tests/test_shell_lexer.py`、`tests/test_help_data.py`

**Interfaces:**
- Consumes: 现有 `parse_line`(shell.py:37)、`_COMMANDS`(shell.py:441)、`_print_command_help`(shell.py:88)、`gray`(term.py:48)
- Produces:
  - `Mode = Literal["home", "chat", "rp", "agent"]`(shell.py 模块级类型别名)
  - `_MODE_COMMANDS: dict[str, Mode] = {"chat": "chat", "rp": "rp", "agent": "agent"}`
  - `_prompt_for_mode(mode: Mode) -> str`(前缀映射)
  - `_placeholder_msg(mode: Mode) -> str`(占位报错文案)
  - `run_shell(_input: Callable[[str], str] = _read_line, initial_mode: Mode = "home") -> None`
  - `HELP_ENTRIES` 增加 chat/rp/agent 三条(help_data.py)

- [ ] **Step 1: 写失败测试**

在 `tests/test_shell.py` 末尾追加:

```python
def test_shell_home_prompt_prefix():
    prompts: list[str] = []

    def _inner(p: str) -> str:
        prompts.append(p)
        return "exit"

    run_shell(_inner)
    assert prompts[0] == "home> "


def test_shell_switch_to_chat_mode(capsys):
    prompts: list[str] = []
    seq = iter(["chat", "你好呀", "/exit", "exit"])

    def _inner(p: str) -> str:
        prompts.append(p)
        try:
            return next(seq)
        except StopIteration:
            raise EOFError from None

    run_shell(_inner)
    out = capsys.readouterr().out
    assert "chat> " in prompts              # 前缀切换为 chat>
    assert "[chat] 对话功能尚未实现" in out    # 普通输入 → 灰色占位报错
    assert "home> " in prompts              # /exit 回到 home


def test_shell_mode_switch_among_modes(capsys):
    prompts: list[str] = []
    seq = iter(["chat", "/rp", "/agent", "/exit", "exit"])

    def _inner(p: str) -> str:
        prompts.append(p)
        try:
            return next(seq)
        except StopIteration:
            raise EOFError from None

    run_shell(_inner)
    assert "chat> " in prompts
    assert "rp> " in prompts
    assert "agent> " in prompts


def test_shell_escape_api_in_chat_mode(capsys, monkeypatch, tmp_path):
    monkeypatch.setattr("rp_agent.storage.DATA_DIR", tmp_path)
    monkeypatch.setattr("rp_agent.api.store.API_DIR", tmp_path / "api")
    run_shell(_feed(["chat", "/api list", "/exit", "exit"]))
    out = capsys.readouterr().out
    assert "(无连接)" in out  # /api list 在 chat 模式可正常执行


def test_shell_exit_in_mode_is_placeholder(capsys):
    run_shell(_feed(["chat", "exit", "/exit", "exit"]))
    out = capsys.readouterr().out
    assert "[chat] 对话功能尚未实现" in out  # 模式内 exit 视为对话内容
    assert out.count("退出") == 1             # 仅最终 home 模式 exit 退出


def test_shell_initial_mode_agent(capsys):
    prompts: list[str] = []
    seq = iter(["hello", "/exit", "exit"])

    def _inner(p: str) -> str:
        prompts.append(p)
        try:
            return next(seq)
        except StopIteration:
            raise EOFError from None

    run_shell(_inner, initial_mode="agent")
    assert prompts[0] == "agent> "  # 从 agent 模式启动
```

同时把 `tests/test_shell.py` 中 `test_help_lists_commands`(第 44-48 行)的循环改为:

```python
    for name in ("help", "config", "reload", "storage", "hello", "history", "exit", "chat", "rp", "agent"):
        assert name in out
```

在 `tests/test_shell_lexer.py` 末尾追加:

```python
def test_mode_commands_are_cmd():
    for name in ("chat", "rp", "agent"):
        assert _tokens(name) == [("class:cmd", name)]
```

在 `tests/test_help_data.py` 末尾追加:

```python
def test_mode_entries_exist():
    for name in ("chat", "rp", "agent"):
        assert find_entry(name) is not None
```

- [ ] **Step 2: 跑测试确认失败**

Run: `uv run pytest tests/test_shell.py tests/test_shell_lexer.py tests/test_help_data.py -q`
Expected: 失败 —— `run_shell` 不接受 `initial_mode` 关键字参数(TypeError);`chat`/`rp`/`agent` 仍是未知命令;HELP_ENTRIES 无对应条目。

- [ ] **Step 3: 实现 shell.py 模式系统**

修改 `src/rp_agent/shell.py`:

3a. 第 6 行 import 改为:

```python
from typing import Callable, Literal
```

3b. 第 32-34 行(PROMPT/_BANNER 常量)替换为:

```python
Mode = Literal["home", "chat", "rp", "agent"]
_MODE_COMMANDS: dict[str, Mode] = {"chat": "chat", "rp": "rp", "agent": "agent"}
_BANNER = "rp-agent 交互式 shell —— 输入 help 查看可用命令;chat/rp/agent 进入 AI 工作模式,模式内 / 转义调用命令,/exit 返回 home,exit 退出"
_history: list[str] = []


def _prompt_for_mode(mode: Mode) -> str:
    return {"home": "home> ", "chat": "chat> ", "rp": "rp> ", "agent": "agent> "}[mode]


def _placeholder_msg(mode: Mode) -> str:
    return f"[{mode}] 对话功能尚未实现(占位模式),/exit 返回 home"
```

3c. 第 452-454 行 `_KNOWN_COMMANDS` 改为(把模式命令并入,供 ShellLexer 着色与 help 参数着色):

```python
_KNOWN_COMMANDS: set[str] = (
    set(_COMMANDS) | {a for e in HELP_ENTRIES for a in e["aliases"]} | set(_MODE_COMMANDS)
)
```

3d. 第 525-554 行 `run_shell` 整体替换为:

```python
def run_shell(
    _input: Callable[[str], str] = _read_line, initial_mode: Mode = "home"
) -> None:
    """交互式主循环。_input 可注入(测试用);Ctrl+C/Ctrl+D 正常退出。

    模式:home 为默认;chat/rp/agent 为 AI 工作模式(占位)。
    非 home 模式下,非 / 开头的输入视为对话内容(占位阶段打印灰色报错);
    / 开头的输入剥掉 / 后走正常命令分派,其中 /exit 返回 home(home 模式 /exit 退出 shell)。
    """
    _history.clear()
    mode = initial_mode
    print(_BANNER)
    while True:
        try:
            line = _input(_prompt_for_mode(mode))
        except (EOFError, KeyboardInterrupt):
            print("退出")
            return
        cmd, args = parse_line(line)
        if not cmd:
            continue
        if line.strip() not in _history:
            _history.append(line.strip())
        escaped = cmd.startswith("/")
        if escaped:
            cmd = cmd[1:]
        if not cmd:
            continue
        if cmd in ("exit", "quit"):
            if escaped and mode != "home":
                mode = "home"
                continue
            if mode == "home":
                print("退出")
                return
            print(gray(_placeholder_msg(mode)))
            continue
        if mode != "home" and not escaped:
            print(gray(_placeholder_msg(mode)))
            continue
        if args == ["--help"]:
            _print_command_help(cmd)
            continue
        if cmd in _MODE_COMMANDS:
            mode = _MODE_COMMANDS[cmd]
            continue
        entry = _COMMANDS.get(cmd)
        if entry is None:
            print(f"未知命令: {cmd}(输入 help 查看可用命令)")
            continue
        try:
            entry[1](args)
        except Exception:
            logger.exception("命令执行失败: %s", cmd)
            print(f"命令执行出错: {cmd}(详情见日志)")
```

3e. 修改 `src/rp_agent/help_data.py`:把 `exit` 条目(第 47-53 行)的 desc 改为:

```python
        "desc": "退出 shell(home 模式);chat/rp/agent 模式内 /exit 返回 home",
```

并在 `api` 条目之后(第 70 行 `},` 之后、列表结尾 `]` 之前)追加三条:

```python
    {
        "command": "chat",
        "aliases": [],
        "desc": "进入 AI 聊天模式(占位)",
        "usage": "chat",
        "params": [],
    },
    {
        "command": "rp",
        "aliases": [],
        "desc": "进入角色扮演模式(占位)",
        "usage": "rp",
        "params": [],
    },
    {
        "command": "agent",
        "aliases": [],
        "desc": "进入 agent 模式(占位)",
        "usage": "agent",
        "params": [],
    },
```

- [ ] **Step 4: 跑测试确认通过**

Run: `uv run pytest tests/test_shell.py tests/test_shell_lexer.py tests/test_help_data.py -q`
Expected: 全部 PASS(含既有用例,如 `test_run_shell_sequence` 未受影响)。

- [ ] **Step 5: 提交**

```bash
git add src/rp_agent/shell.py src/rp_agent/help_data.py tests/test_shell.py tests/test_shell_lexer.py tests/test_help_data.py
git commit -m "feat: shell 工作模式系统(chat/rp/agent 模式、/ 转义、/exit 回 home)"
```

---

### Task 2: core 占位模块 + CLI 子命令

**Files:**
- Create: `src/rp_agent/core/chat.py`、`src/rp_agent/core/rp.py`、`src/rp_agent/core/agent.py`
- Modify: `src/rp_agent/cli.py`(app 注册三个子命令)
- Test: `tests/test_core.py`(新建)、`tests/test_cli.py`

**Interfaces:**
- Consumes: Task 1 的 `run_shell(_input, initial_mode)`(shell.py)
- Produces:
  - `rp_agent.core.chat.run() -> None`、`rp_agent.core.rp.run() -> None`、`rp_agent.core.agent.run() -> None`
  - cli 子命令 `chat` / `rp` / `agent`(Typer,无参数)

- [ ] **Step 1: 写失败测试**

新建 `tests/test_core.py`:

```python
from rp_agent.core.agent import run as agent_run
from rp_agent.core.chat import run as chat_run
from rp_agent.core.rp import run as rp_run


def _capture_run_shell(monkeypatch) -> dict:
    captured: dict = {}
    monkeypatch.setattr(
        "rp_agent.shell.run_shell",
        lambda _input=None, initial_mode="home": captured.update(mode=initial_mode),
    )
    return captured


def test_chat_run_enters_chat_mode(monkeypatch):
    captured = _capture_run_shell(monkeypatch)
    chat_run()
    assert captured.get("mode") == "chat"


def test_rp_run_enters_rp_mode(monkeypatch):
    captured = _capture_run_shell(monkeypatch)
    rp_run()
    assert captured.get("mode") == "rp"


def test_agent_run_enters_agent_mode(monkeypatch):
    captured = _capture_run_shell(monkeypatch)
    agent_run()
    assert captured.get("mode") == "agent"
```

在 `tests/test_cli.py` 末尾追加:

```python
def test_chat_command_registered(monkeypatch):
    called: dict = {}
    monkeypatch.setattr("rp_agent.core.chat.run", lambda: called.update(ok=True))
    result = runner.invoke(app, ["chat"])
    assert result.exit_code == 0
    assert called.get("ok") is True


def test_rp_command_registered(monkeypatch):
    called: dict = {}
    monkeypatch.setattr("rp_agent.core.rp.run", lambda: called.update(ok=True))
    result = runner.invoke(app, ["rp"])
    assert result.exit_code == 0
    assert called.get("ok") is True


def test_agent_command_registered(monkeypatch):
    called: dict = {}
    monkeypatch.setattr("rp_agent.core.agent.run", lambda: called.update(ok=True))
    result = runner.invoke(app, ["agent"])
    assert result.exit_code == 0
    assert called.get("ok") is True
```

- [ ] **Step 2: 跑测试确认失败**

Run: `uv run pytest tests/test_core.py tests/test_cli.py -q`
Expected: 失败 —— `ModuleNotFoundError: No module named 'rp_agent.core.chat'`;cli 无 chat/rp/agent 命令(exit_code 2)。

- [ ] **Step 3: 实现 core 占位模块与 cli 子命令**

新建 `src/rp_agent/core/chat.py`:

```python
"""chat 模式:AI 聊天(占位,未来接 client.chat)。"""


def run() -> None:
    from rp_agent.shell import run_shell

    run_shell(initial_mode="chat")
```

新建 `src/rp_agent/core/rp.py`:

```python
"""rp 模式:角色扮演(占位)。"""


def run() -> None:
    from rp_agent.shell import run_shell

    run_shell(initial_mode="rp")
```

新建 `src/rp_agent/core/agent.py`:

```python
"""agent 模式:agent 工作方式(占位)。"""


def run() -> None:
    from rp_agent.shell import run_shell

    run_shell(initial_mode="agent")
```

在 `src/rp_agent/cli.py` 的 `shell` 命令(第 116-121 行)之后追加:

```python
@app.command()
def chat() -> None:
    """进入 AI 聊天模式(占位,未来接入 client.chat)。"""
    from rp_agent.core.chat import run

    run()


@app.command()
def rp() -> None:
    """进入角色扮演模式(占位)。"""
    from rp_agent.core.rp import run

    run()


@app.command()
def agent() -> None:
    """进入 agent 模式(占位)。"""
    from rp_agent.core.agent import run

    run()
```

- [ ] **Step 4: 跑测试确认通过**

Run: `uv run pytest tests/test_core.py tests/test_cli.py -q`
Expected: 全部 PASS。再跑全量回归:`uv run pytest -q`,Expected: 全部 PASS(现 68+ 用例)。

- [ ] **Step 5: 提交**

```bash
git add src/rp_agent/core/chat.py src/rp_agent/core/rp.py src/rp_agent/core/agent.py src/rp_agent/cli.py tests/test_core.py tests/test_cli.py
git commit -m "feat: chat/rp/agent CLI 子命令与 core 占位模块"
```

---

## 验证方式(全部完成后)

```bash
uv run pytest -v          # 全量测试通过
uv run rp-agent chat      # 手动:进入 chat 模式,前缀 chat>,输入文本见灰色占位报错,/api list 可执行,/exit 回 home,exit 退出
uv run rp-agent --help    # 帮助里出现 chat/rp/agent 子命令
```

## 行为约定(实现时须保持)

- 模式内 `/` 转义可调用全部正常命令(`/api`、`/help`、`/config` 等)
- 模式内 `exit`/`quit`(不带 `/`)视为对话内容 → 占位报错,不退出
- `chat --help`(带 `--help`)在任意模式显示 chat 帮助条目(help_data 已注册)
- home 模式 `/exit` 与 `exit` 均退出整个 shell
