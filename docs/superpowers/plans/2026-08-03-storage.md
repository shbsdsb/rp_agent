# 储存链路实施计划(基础设施层)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 构建储存链路基础设施:项目根 `data/` 目录管理、JSON 读写封装(原子写)、安全路径校验(防穿越),为角色卡/聊天/预设/API 数据提供统一存储底座。

**Architecture:** 单文件 `src/rp_agent/storage.py`:模块级目录常量(`DATA_DIR` 由包路径 `parents[2]` 推导)+ `ensure_dirs()` + `json_read`/`json_write`(临时文件+`os.replace` 原子写)+ `safe_path`(拒绝 `..` 与绝对路径)。`data/` 加入 `.gitignore`。

**Tech Stack:** Python 3.14、标准库(json/os/pathlib/logging)、pytest。

## Global Constraints

- Python >= 3.14;测试一律 `uv run pytest`(Windows 环境,不用 python3)
- 不新增依赖(标准库实现);`uv.lock` 不变
- `DATA_DIR` 固定为项目根/data(通过 `Path(__file__).resolve().parents[2] / "data"` 推导,不依赖 cwd、不可配置)
- 所有 data 读写统一走 `storage.py`,禁止散落裸 `open()` 操作 data
- `data/` 不入 git(`.gitignore` 追加)
- 现有 22 项测试保持通过
- 工作分支 `feat/storage`

---

### Task 1: `storage.py` 基础设施 + `data/` 入 gitignore

**Files:**
- Create: `src/rp_agent/storage.py`
- Create: `tests/test_storage.py`
- Modify: `.gitignore`

**Interfaces:**
- Consumes: 无(仅标准库)
- Produces:
  - `DATA_DIR: Path`、`CHARACTERS_DIR / CHATS_DIR / PRESETS_DIR / API_DIR: Path`
  - `ensure_dirs() -> None`(幂等创建 data + 四子目录)
  - `json_read(path: Path) -> object | None`(缺失/损坏返回 None + 告警)
  - `json_write(path: Path, data: object) -> None`(utf-8、ensure_ascii=False、indent=2、原子写、失败告警不崩溃)
  - `safe_path(relative: str) -> Path`(拒绝绝对路径与含 `..` 的路径,抛 `ValueError`;合法路径 resolve 到 DATA_DIR 下)

- [ ] **Step 1: 写失败测试 `tests/test_storage.py`**

```python
from pathlib import Path

import pytest

from rp_agent import storage


def test_data_dir_points_to_project_root():
    expected = Path(__file__).resolve().parents[1] / "data"
    assert storage.DATA_DIR == expected


def test_ensure_dirs_creates_subdirs(monkeypatch, tmp_path):
    monkeypatch.setattr(storage, "DATA_DIR", tmp_path)
    storage.ensure_dirs()
    for name in ("characters", "chats", "presets", "api"):
        assert (tmp_path / name).is_dir()
    storage.ensure_dirs()  # 幂等,不抛错


def test_json_write_read_roundtrip(tmp_path):
    p = tmp_path / "nested" / "data.json"
    storage.json_write(p, {"a": 1, "b": [1, 2]})
    assert storage.json_read(p) == {"a": 1, "b": [1, 2]}


def test_json_read_missing_returns_none(tmp_path):
    assert storage.json_read(tmp_path / "nope.json") is None


def test_json_read_broken_returns_none(tmp_path):
    p = tmp_path / "broken.json"
    p.write_text("{not json", encoding="utf-8")
    assert storage.json_read(p) is None


def test_safe_path_normal(monkeypatch, tmp_path):
    monkeypatch.setattr(storage, "DATA_DIR", tmp_path)
    result = storage.safe_path("characters/alice.json")
    assert result == (tmp_path / "characters" / "alice.json").resolve()


@pytest.mark.parametrize("bad", ["../evil.json", "a/../../evil.json"])
def test_safe_path_rejects_traversal(monkeypatch, tmp_path, bad):
    monkeypatch.setattr(storage, "DATA_DIR", tmp_path)
    with pytest.raises(ValueError):
        storage.safe_path(bad)
```

- [ ] **Step 2: 运行测试确认失败**

```bash
uv run pytest tests/test_storage.py -v
```
Expected: FAIL(`ModuleNotFoundError: No module named 'rp_agent.storage'`)

- [ ] **Step 3: 写 `src/rp_agent/storage.py` 并追加 `.gitignore`**

`src/rp_agent/storage.py`:
```python
"""储存链路基础设施:data 目录管理、JSON 读写、安全路径校验。"""
from __future__ import annotations

import json
import logging
import os
from pathlib import Path

logger = logging.getLogger("rp_agent")

# 项目根/data(通过包路径推导:src/rp_agent/storage.py -> 项目根)
DATA_DIR = Path(__file__).resolve().parents[2] / "data"
CHARACTERS_DIR = DATA_DIR / "characters"
CHATS_DIR = DATA_DIR / "chats"
PRESETS_DIR = DATA_DIR / "presets"
API_DIR = DATA_DIR / "api"

_SUB_NAMES = ("characters", "chats", "presets", "api")


def ensure_dirs() -> None:
    """创建 data 及四个子目录,幂等(动态基于 DATA_DIR,便于测试注入)。"""
    DATA_DIR.mkdir(parents=True, exist_ok=True)
    for name in _SUB_NAMES:
        (DATA_DIR / name).mkdir(parents=True, exist_ok=True)


def json_read(path: Path) -> object | None:
    """读 JSON;文件缺失/损坏返回 None 并告警,不崩溃。"""
    try:
        with path.open("r", encoding="utf-8") as f:
            return json.load(f)
    except (OSError, json.JSONDecodeError) as exc:
        logger.warning("读取 JSON 失败(%s): %s", path, exc)
        return None


def json_write(path: Path, data: object) -> None:
    """原子写 JSON(临时文件 + os.replace);失败告警不崩溃。"""
    path.parent.mkdir(parents=True, exist_ok=True)
    tmp_path = path.with_suffix(path.suffix + ".tmp")
    try:
        with tmp_path.open("w", encoding="utf-8") as f:
            json.dump(data, f, ensure_ascii=False, indent=2)
        os.replace(tmp_path, path)
    except OSError as exc:
        logger.error("写入 JSON 失败(%s): %s", path, exc)
        try:
            tmp_path.unlink(missing_ok=True)
        except OSError:
            pass


def safe_path(relative: str) -> Path:
    """将相对路径解析到 data 根;拒绝绝对路径与 .. 逃逸,抛 ValueError。"""
    rel = Path(relative)
    if rel.is_absolute() or ".." in rel.parts:
        raise ValueError(f"非法路径(禁止目录穿越): {relative}")
    return (DATA_DIR / rel).resolve()
```

`.gitignore` 追加(在 `# Reasonix 工具本地目录` 之前插入):
```gitignore
# 运行时数据
data/
```

- [ ] **Step 4: 运行测试确认通过**

```bash
uv run pytest tests/test_storage.py -v
```
Expected: 8 passed(6 个函数,其中 `test_safe_path_rejects_traversal` 参数化展开为 2 个用例)

- [ ] **Step 5: 提交**

```bash
git add src/rp_agent/storage.py tests/test_storage.py .gitignore
git commit -m "feat: 储存链路基础设施(storage.py:data 目录/JSON 读写/安全路径)"
```

---

### Task 2: 全量验证与收尾

**Files:**
- Modify: `README.md`(补充数据目录说明)

**Interfaces:**
- Consumes: Task 1 产物
- Produces: 全绿测试套件 + 完整 git 历史

- [ ] **Step 1: 运行全量测试**

```bash
uv run pytest -v
```
Expected: 30 passed(22 原有 + 8 新)

- [ ] **Step 2: 冒烟验证 data 目录自动创建**

```bash
uv run python -c "from rp_agent.storage import ensure_dirs, DATA_DIR; ensure_dirs(); print(DATA_DIR); import os; print(sorted(os.listdir(DATA_DIR)))"
```
Expected: 打印项目根 data 路径与四个子目录;`data/` 已被 gitignore(git status 不显示)。

- [ ] **Step 3: 更新 README.md(在"热重载"章节后追加)**

```markdown
## 数据存储

项目根 `data/` 目录(不入 git),运行时数据:

- `characters/` — 角色卡
- `chats/` — 聊天记录
- `presets/` — RP 预设
- `api/` — API 连接配置

所有 data 读写统一走 `src/rp_agent/storage.py`(`json_read`/`json_write`/`safe_path`)。
```

- [ ] **Step 4: 确认工作树整洁并提交收尾**

```bash
git status --short
git add -A
git commit -m "chore: 储存链路完成(README 数据目录说明)"
git log --oneline
```
Expected: `git log --oneline` 显示连续提交历史。

---

## 验收清单(对照 spec)

- [ ] `DATA_DIR` 指向项目根/data,四个子目录常量存在 → spec §3.1、§3.2
- [ ] `ensure_dirs()` 幂等创建 data + 四子目录 → spec §3.2
- [ ] `json_read` 缺失/损坏返回 None + 告警 → spec §3.2、§4
- [ ] `json_write` 原子写、失败不崩溃 → spec §3.2、§4
- [ ] `safe_path` 拒绝 `..` 逃逸(ValueError)→ spec §3.2、§4
- [ ] `.gitignore` 含 `data/` → spec §6
- [ ] 现有 22 项测试保持通过,总计 30 passed → spec §7
